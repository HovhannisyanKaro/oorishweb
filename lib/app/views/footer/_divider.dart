part of 'footer_view.dart';

class _Divider extends StatelessWidget {
  const _Divider({super.key});

  @override
  Widget build(BuildContext context) {
    return Divider(height: 56, color: Colors.white.withValues(alpha: 0.5));
  }
}
